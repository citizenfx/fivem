import s from './item.module.scss';

export const itemsStyles = {
  wrapper: s['item-wrapper'],
  item: s.item,
  itemLabelled: s.labelled,
  itemSelected: s.selected,
  itemActive: s.active,
  itemIcon: s.icon,
  itemTitle: s.title,
  itemStatus: s.status,
  itemStatusEntry: s.entry,
  creator: s.creator,
  children: s.children,

  dropping: s.dropping,
  dragging: s.dragging,
};
